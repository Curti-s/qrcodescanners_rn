import * as React from 'react';
import {
  View,
  Text,
} from 'react-native';

export default () => {
  return (
    <View style={{ flex:1 }}>
      <Text>Bulk Scan screen</Text>
    </View>
  );
}
