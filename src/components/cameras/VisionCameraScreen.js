import React from 'react';
import {
  View,
  Text,
} from 'react-native';

export default ({ navigation }) => {
  return (
    <View style={{ flex:1 }}>
      <Text>vision camera screen</Text>
    </View>
  );
}
